<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>io.broessl</groupId>
	<artifactId>treesql-swing-gui</artifactId>
	<version>${treesql.version}</version>
	<parent>
		<artifactId>treesql-parent</artifactId>
		<groupId>io.broessl</groupId>
		<version>${treesql.version}</version>
		<relativePath>../pom.xml</relativePath>
	</parent>
	<properties>
		<mainClass>io.broessl.treesql.gui.Main</mainClass>
	</properties>
	<dependencies>
		<!-- TreeSQL Core Dependencies -->
		<dependency>
			<artifactId>treesql-core</artifactId>
			<groupId>io.broessl</groupId>
			<version>${treesql.version}</version>
		</dependency>
		<dependency>
			<artifactId>treesql-file</artifactId>
			<groupId>io.broessl</groupId>
			<version>${treesql.version}</version>
		</dependency>
		<dependency>
			<artifactId>treesql-xml</artifactId>
			<groupId>io.broessl</groupId>
			<version>${treesql.version}</version>
		</dependency>
		<dependency>
			<artifactId>treesql-yaml</artifactId>
			<groupId>io.broessl</groupId>
			<version>${treesql.version}</version>
		</dependency>
		<dependency>
			<artifactId>treesql-csv</artifactId>
			<groupId>io.broessl</groupId>
			<version>${treesql.version}</version>
		</dependency>
		
		<!-- Swing UI Enhancement Libraries -->
		<dependency>
			<groupId>com.formdev</groupId>
			<artifactId>flatlaf</artifactId>
			<version>3.5.4</version>
		</dependency>
		
		<!-- RSyntaxTextArea for code highlighting -->
		<dependency>
			<groupId>com.fifesoft</groupId>
			<artifactId>rsyntaxtextarea</artifactId>
			<version>3.5.3</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Maven Compiler Plugin -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<source>21</source>
					<target>21</target>
					<encoding>UTF-8</encoding>
				</configuration>
			</plugin>
			
			<!-- Maven Assembly Plugin for creating executable JAR -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>3.7.1</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<archive>
						<manifest>
							<mainClass>${mainClass}</mainClass>
						</manifest>
					</archive>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
				</configuration>
			</plugin>
			
			<!-- Maven Shade Plugin (alternative to assembly) -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.6.0</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<transformers>
								<transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<mainClass>${mainClass}</mainClass>
								</transformer>
							</transformers>
							<createDependencyReducedPom>false</createDependencyReducedPom>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
